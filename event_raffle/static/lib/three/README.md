# three.js (vendored)

- `three.min.js` — three.js **r128**, build global UMD (`window.THREE`).
  Source: https://unpkg.com/three@0.128.0/build/three.min.js

Loaded lazily as the `event_raffle.assets_three` bundle; the wheel scene uses
only the core `window.THREE`. Tux is built procedurally (no model loader).
